import SwiftUI

/// Pagina delle schede: vuota, in attesa della nuova implementazione.
struct SheetListView: View {
    var body: some View {
        NavigationStack {
            List {}
                .navigationTitle("Schede")
                .navigationBarTitleDisplayMode(.inline)
        }
    }
}
